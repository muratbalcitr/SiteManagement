<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    
    <item
        android:id="@+id/action_search"
        android:icon="@android:drawable/ic_menu_search"
        android:title="Ara"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="ifRoom|collapseActionView"
        app:iconTint="?attr/colorOnSurface" />
    
    <item
        android:id="@+id/action_export_excel"
        android:icon="@android:drawable/ic_menu_upload"
        android:title="@string/export_to_excel"
        app:showAsAction="always"
        app:iconTint="?attr/colorOnSurface" />
    
    <item
        android:id="@+id/action_create_fees_for_all"
        android:icon="@drawable/outline_attach_money_24"
        android:title="@string/create_fees_for_all_units_title"
        app:showAsAction="always"
        app:iconTint="?attr/colorOnSurface" />
</menu>

